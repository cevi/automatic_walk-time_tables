version: "3.9"

services:

  tile_cache:
    image: registry.test.emeal.ch/cevi/awtg_tile_caching:dev

  mapfish:
    image: registry.test.emeal.ch/cevi/awtg_mapfish:dev

  swiss_tml:
    image: registry.test.emeal.ch/cevi/swiss_tml_api:dev

  backend:
   image: registry.test.emeal.ch/cevi/awtg_backend:dev

  webinterface:
    image: registry.test.emeal.ch/cevi/awtg_webinterface:dev
